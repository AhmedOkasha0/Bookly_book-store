import 'package:flutter/material.dart';

class ThemeScreen {
  static const primaryColor = Color(0xff100B20);
  static ThemeData darkMode =
      ThemeData(scaffoldBackgroundColor: primaryColor, textTheme: TextTheme());
}
